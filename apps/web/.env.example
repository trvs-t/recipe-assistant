# Optional for local development. If either value is missing, the app uses
# its explicit demo adapter with local sample recipes and no network calls.
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
